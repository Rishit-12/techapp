package com.luv2code.ecommerce.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MyAppConfig {
    // Application-level configuration is intentionally kept minimal.
    // CORS and security configuration live in SecurityConfiguration.
}
